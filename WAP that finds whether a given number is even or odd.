#include <stdio.h>
#include <math.h>

int main()
{
    int num1;
    printf("enter value of num1");
    scanf("%d",&num1);
    if(num1 % 2 == 0){
        printf("given number is  even number:%d\n",num1);
    }
    else{
        printf("given number is odd number:%d\n",num1);
    }
    return 0;
}
