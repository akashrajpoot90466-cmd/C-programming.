#include <stdio.h>

int main() {
    int num,i,sum=0;
    printf("enter any number");
    scanf("%d",&num);
    for(i=1;i<=num;i=i+1){
        sum=sum+i;
    }
    printf("sum=%d\n",sum);
    return 0;
    }
