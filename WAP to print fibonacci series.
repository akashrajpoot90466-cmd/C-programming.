#include <stdio.h>

int main() {
    int i,n;
    int a=0,b=1,next;
    printf("enter no of terms");
    scanf("%d",&n);
    for(i=0;i<=n;i=i+1){
    printf("%d",a);
    next = a+b;
    a=b;
    b=next;
    }
    return 0;
}
