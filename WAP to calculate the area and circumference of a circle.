#include <stdio.h>
#include <math.h>

int main()
{
 float radius,area,circum,pi;
 printf("enter value of radius");
 scanf("%f",&radius);
 pi = 3.14;
 area = (pi*radius*radius);
 circum = (2*pi*radius);
 printf("%f",area);
 printf("%f",circum);
 return 0;
}
